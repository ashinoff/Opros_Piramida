# CLAUDE.md — Опрос ПУ (аналитика собираемости приборов учёта)

Веб-приложение: принимает выгрузку «Опрос ПУ» из ПО «Пирамида» (xlsx, ~130 тыс. строк,
12–15 МБ), ведёт реестр приборов учёта (ПУ) и показывает собираемость (% опроса)
в разрезах. Задеплоено на Амвере, встраивается iframe'ом в оболочку «ПО Платформа»
(https://github.com/ashinoff/SUE_system).

Общаться с пользователем и писать комментарии/интерфейс — на русском.
Домен: энергетика, Сочинские электросети. Пользователи — не программисты.

## Стек и запуск

- Python 3.12, FastAPI, SQLAlchemy 2, PostgreSQL (prod, CNPG на Амвере) / SQLite (локально)
- Фронт: SPA на vanilla JS + Chart.js (CDN), файлы в `static/`, без сборки
- Excel-экспорт: xlsxwriter
- Локальный запуск: `DATA_DIR=./data python -m uvicorn app.main:app --port 8000`
  (без DATABASE_URL работает на SQLite — удобно для отладки)
- Prod: Dockerfile + `amvera.yml` (containerPort 80, persistenceMount /data)

### Переменные окружения
| Переменная | Назначение |
|---|---|
| `DATABASE_URL` | prod: `postgresql://Ashinoff:...@amvera-ashinoff-cnpg-oprosdb-rw:5432/Oprosdb` — регистр имён важен! |
| `DATA_DIR` | каталог для копий загруженных xlsx (default `/data`, на Амвере постоянный диск) |
| `ADMIN_PASSWORD` | пароль admin при первом старте (default `admin`) |
| `MAX_UPLOAD_MB` | лимит размера загрузки (default 50) |

## Структура кода

```
app/
  main.py      — все API-маршруты (FastAPI), статика
  db.py        — модели: User, Upload, Meter, MeterState, Change, Task; выбор БД
  parser.py    — потоковый разбор xlsx (openpyxl read_only), классификаторы
  importer.py  — импорт загрузки в фоне, дифф с предыдущей, автозакрытие заданий
  analytics.py — все отчёты (сводка, СПОДЭС, маршруты, мёртвые ТП, приоритеты...)
  auth.py      — сессии (in-memory, cookie/Bearer), роли, ограничение по РЭС
  export.py    — Excel-отчёты с диаграммами
static/
  index.html, app.js, style.css — SPA, вкладки по ролям
```

## Формат входного файла (стабилен, из Пирамиды)

Лист «ПОДРОБНО». Строки 1–9 — шапка (период, итоги), строка 10 — заголовки,
данные с 11-й. Парсер ищет лист по подстроке «ПОДРОБ» и строку с 'ПУ','Тип ПУ'.

Колонки (0-индексация в row):
- B(1) ПУ, C(2) Тип ПУ («(СПОДЭС)» в названии → is_spodes; производитель — по префиксу,
  правила в `parser.VENDOR_RULES`), D(3) Серийный номер
- E(4) Фазность, F(5) Маршрут опроса → `classify_route`: МКС / RootRouter / RTR /
  GSM (телефоны) / IP-шлюз (голые IP) / Без порта / Прочее; часть до « - » = ведущее устройство
- G(6) Дата опроса, H(7) Точка учёта (адресный путь — КЛЮЧ для логики «отремонтировано»)
- I(8) Область, J(9) **РЭС** (8 шт.: Адлерский, Сочинский, Туапсинский, Дагомысский,
  Хостинский, Лазаревский, Краснополянский, ПС РЭС)
- K(10) **Фидер**, L(11) **ТП** ← внимание: ТП именно в L, не в K!
- M(12) Тип абонента (ФЛ/ЮЛ/НЕТ ПРИВЯЗКИ), N(13) Тип ТУ, O(14) Отключен
- P(15) **Собираемость** («Собирается» / пусто), Q(16) Угасание сбора,
  R(17) Модуляция, S(18) Тип данных
- Строки могут быть короче 19 ячеек — парсер дополняет None; дубли ключа внутри
  файла пропускаются (первая строка побеждает)

Ключ ПУ в реестре: `meter_key = Тип ПУ || Серийный` (уникальный в Meter).

## Бизнес-логика (важно не сломать)

- **Загрузка = снимок.** Upload хранит итоги, MeterState — состояние каждого ПУ
  в этой загрузке. Meter — «сухой остаток» (реестр) с active/replaced_by.
- **Дифф** (importer.py): added / removed / collect_lost / collect_restored / moved.
- **«Отремонтировано»**: на той же Точке учёта (колонка H) старый ПУ исчез, появился
  новый с другим серийником, и он собирается → `repaired`; если не собирается →
  `serial_changed`. Пары строятся zip'ом выбывших/добавленных по tu_path.
- **Мёртвая ТП** = на ТП ≥2 ПУ и ни один не собирается → «не работает ведущее
  устройство» (показываем route_device).
- **Приоритеты для участков**: 1 — мёртвые ТП, 2 — угасание сбора (кол. Q),
  3 — одиночные ПУ без опроса не на мёртвых ТП.
- **Задания** (Task): создаёт staff/admin/uploader, авто-закрываются при импорте,
  если ПУ задания начал собираться.
- **Роли**: admin (всё), uploader (всё кроме удаления БД и пользователей),
  staff — служба учёта (видит всё, задания, не грузит), res — участок (ТОЛЬКО свой
  РЭС — принудительно в `auth.visible_res` + `_resolve` в main.py, фильтр на бэке,
  не доверять фронту).
- Обработка файла — фоновый поток, статус processing/done/error, фронт поллит
  `/api/uploads`. Параллельный импорт запрещён (409).

## Тестирование

- Реальный файл ~130 728 ПУ импортируется за ~100 сек (SQLite), итоги должны
  сходиться с шапкой файла (строки 4–8: всего/в опросе/без опроса).
- Для диффа есть приём: скопировать реальный файл и внести контролируемые правки
  (замена серийника на том же адресе ± опрос, удаление строк, новые строки) —
  проверить типы изменений в `/api/report/changes`.
- Быстрый smoke: логин admin → загрузка → summary → export/summary (xlsx открывается).

## TODO (согласовано с пользователем, делать по запросу)

1. Согласование добавления новых ПУ чекбоксами (сейчас автодобавление + вкладка
   «Изменения»): нужна таблица pending + UI подтверждения для загрузчика.
2. ~~Интеграция авторизации с Платформой~~ — СДЕЛАНО (см. «Интеграция с Платформой»
   ниже и журнал 2026-07-16).
3. Экспорт отчётов в Word (сейчас только Excel).
4. Автоочистка старых xlsx и MeterState при росте базы (отчёты по датам сохранять).
5. **[СОГЛАСОВАНО 2026-07-16, делать в следующих итерациях] Смена модели хранения.**
   Сейчас каждая загрузка = полный снимок: `MeterState` пишет ~130к строк на каждую
   выгрузку (растёт линейно). Пользователь хочет: БАЗУ (`Meter`) наполнить с ПЕРВОЙ
   выгрузки и далее только **дополнять/изменять** (новые ПУ добавлять, у существующих
   менять статус/поля), НЕ храня по 130к строк каждую неделю. Хранить только:
   актуальное состояние каждого ПУ в `Meter` + **журнал изменений** (`Change`, уже есть)
   + **периодические агрегаты статистики** (собираемость по РЭС/СПОДЭС/маршрутам на дату
   загрузки — маленькие сводки, копятся еженедельно для истории/графиков). То есть уйти
   от снимка `MeterState`-на-загрузку к «текущее состояние + дифф-журнал + сохранённые
   сводки». Продумать, чтобы отчёты «на дату» строились из сохранённых агрегатов, а не
   из полного снимка. НЕ ломать текущую логику диффа (added/removed/repaired/…).

## Интеграция с Платформой (SUE_system, Keycloak SSO)

Приложение зарегистрировано в реестре Платформы (`SUE_system/src/config/apps.js`,
id `opros`, иконка `OprosTile` в `appIcons.jsx`). Домен Amvera:
`opros-piramida-ashinoff.amvera.io`.

**Схема (как у СИЗ/Светлячка):** Keycloak решает «кто ты» (по **email**) + «пускать
ли» (realm-роль **`opros-user`**). Приложение решает «что можно» — роль/РЭС из своей
БД по email, НЕ из токена. Один пользователь в Keycloak = одна роль доступа + email.

- Бэк: `app/config.py` (env), `app/keycloak.py` (проверка токена по JWKS: подпись/iss/
  exp/azp==web-desktop, aud не требуем), `auth.resolve_platform_user()` (гейт `opros-user`,
  связка по keycloak_id→email), эндпоинт `POST /api/auth/platform` (обмен на свою сессию,
  формат как `/api/login`). CSP `frame-ancestors` — middleware в `main.py`.
- Фронт (`static/app.js`): слушает `postMessage {type:'platform-auth',token}` ТОЛЬКО от
  `PLATFORM_ORIGIN`, шлёт `{type:'app-ready'}` родителю, меняет токен на сессию, fallback
  на обычный вход. Токен сессии хранится в `localStorage(opros_token)` и шлётся как
  `Bearer` (чтобы работать во встроенном iframe, где Lax-cookie не уходит на XHR).
- Env на Amvera для включения: `PLATFORM_SSO=true` (KEYCLOAK_ISSUER/JWKS_URL/AZP,
  PLATFORM_ORIGIN, OPROS_ACCESS_ROLE имеют дефолты под наш realm). БД: у User добавлены
  `email`, `keycloak_id` (лёгкая миграция `_ensure_user_columns` в `db.py`).
- В учётках приложения (вкладка «Пользователи») проставить email = email в Keycloak.

## Журнал изменений

- **2026-07-16** — Загрузка: чанковая отправка + прогресс-бар (причина «база не
  наполнилась»). Файл выгрузки 12–15 МБ не проходил одним POST — прокси Amvera
  режет большие тела (~413), запрос не доходил до бэкенда → импорт не запускался.
  Добавлены эндпоинты `POST /api/upload/begin` → `POST /api/upload/chunk/{token}`
  (raw-body по 512 КБ, дописывается во временный `tmp_<token>.part`) →
  `POST /api/upload/complete/{token}` (собирает, создаёт Upload, запускает импорт).
  Старый `/api/upload` оставлен. Фронт (`sendFile`) режет файл на куски 512 КБ,
  показывает **прогресс-бар** (`showUp`/`pollUpload`): % отправки → анимированный
  индикатор «обработка» → «Готово: N ПУ» / ошибка (стили `.up-progress/.up-bar`).
  Плюс детект строки заголовков в `parser.py` теперь устойчив к пробелам
  (`strip()`). Проверено end-to-end на мини-файле: begin→chunk→complete→импорт
  `done`, реестр наполнился.
- **2026-07-16** — Вход/роли/РЭС. (1) БАГ SSO: при ПЕРВОМ входе через Платформу
  (связка учётки по email) `resolve_platform_user` делал `db.commit()` →
  `expire_on_commit=True` обесценивал атрибуты `user`, затем `expunge`/`close` → в
  `_user_dict(user)` `DetachedInstanceError` → 500 → фронт откатывался на форму
  входа (симптом «не логинит автоматически, выкидывает на меню входа»). Добавлен
  `db.refresh(user)` после commit. Проверено TestClient (первый и повторный вход
  → 200). (2) Роль **`chief` («Начальник»)** — только просмотр всех РЭС (все
  view-вкладки, без загрузки/пользователей/создания заданий; права расширим
  позже). Добавлена в `ROLES` (db.py), `roleName`, меню (NAV) и форму пользователя.
  (3) **7 стандартных РЭС** (`STANDARD_RES` в db.py) — чтобы назначать учётки ещё
  ДО первой выгрузки; `res_list` теперь = объединение стандартных + реально
  встретившихся в `Meter.res`. ⚠️ Названия РЭС должны совпадать с колонкой J
  выгрузки (сейчас: Адлерский/Дагомысский/Краснополянский/Лазаревский/Сочинский/
  Туапсинский/Хостинский — сверить с реальными данными).
- **2026-07-16** — Радикальный визуальный редизайн фронта (vanilla JS, `static/`).
  Ушли от эмодзи → своя система inline-SVG иконок (`ICONS`/`ic()` в `app.js`, стиль
  как в СИЗ: 24×24, currentColor, stroke). Вместо верхних вкладок — **боковое меню**
  (сайдбар) с разделами (Обзор/Аналитика/Реестр/Администрирование), у каждого пункта
  SVG-иконка со свечением на hover/active. Тема — **градиент фиолетовый→чёрный**
  (`--sidebar-g1/2/3`, акцент `--violet #8b5cf6`), primary-кнопки и фокусы —
  фиолетовые. Добавлена фирменная **заставка загрузки РОССЕТИ** (`.rosseti-letter`,
  электрическое свечение, буквы с задержкой; #loading в `index.html`, `hideLoader()`).
  Скроллбар меню — в тон (фиолетовый, полупрозрачный). Логотип приложения — белая
  ступенчатая пирамида (`BRAND_PYRAMID`) в сайдбаре + фиолетово-чёрная плитка
  (`BRAND_TILE`) на входе; на Платформе `OprosTile` перекрашен в фиолетово-чёрный
  градиент. `renderNav`/`openTab` (заголовок страницы в топбаре, active-подсветка),
  `markChangesDot` (точка уведомления на пункте «Изменения»). Только фронт +
  `appIcons.jsx` на Платформе; бэкенд не менялся.
- **2026-07-16** — Полная интеграция с Платформой + фиксы. (1) БАГ ВХОДА: в
  `static/app.js` обработчик `l_pass.onkeydown` возвращал `false` при любой клавише
  кроме Enter → браузер отменял ввод символа → в поле пароля нельзя было печатать.
  Исправлено (реагируем только на Enter, ничего не возвращаем). (2) Учётки: у User
  добавлен `email` (+ `keycloak_id`), список сортируется по алфавиту (имя/логин,
  casefold), в модалке и таблице «Пользователи» — поле/колонка Email. (3) SSO:
  `config.py`, `keycloak.py`, `auth.resolve_platform_user`, `POST /api/auth/platform`,
  CSP-middleware; фронт делает обмен токена и Bearer-авторизацию. `requirements.txt`:
  `python-jose[cryptography]`. Платформа: `OprosTile` (фиолетовая пирамида) + запись
  `opros` в `apps.js`, env `VITE_APP_OPROS_URL`.

## Грабли, на которые уже наступали

- PostgreSQL чувствителен к регистру: база `Oprosdb`, пользователь `Ashinoff` —
  с большой буквы.
- В выгрузке встречаются дубли ключа тип+серийник — не падать (обработано).
- Короткие строки xlsx (обрезанные хвостовые пустые ячейки) — парсер дополняет.
- ТП — колонка L; K — фидер (пользователь сначала думал наоборот).
- Не использовать сырой SQL — код должен работать и на SQLite, и на PostgreSQL.
- **Amvera режет большие тела запросов (~413 на >~10 МБ).** Файл выгрузки 12–15 МБ
  грузить ТОЛЬКО чанками (`/api/upload/begin|chunk|complete`), не одним POST.
